---
layout: "@/layouts/block-docs-layout.astro"
title: VECTOR_MAX
---

import docstring from "@blocks/TRANSFORMERS/VECTOR_MANIPULATION/VECTOR_MAX/docstring.json";
import app from "@blocks/TRANSFORMERS/VECTOR_MANIPULATION/VECTOR_MAX/app.json";
import PythonDocsDisplay from "@/components/python-docs-display.astro";
import AppDisplay from "@/components/app-display.tsx";
import GetHelpWidget from "@/components/get-help-widget.astro";

<PythonDocsDisplay docstring={docstring} />

<details>
<summary>Python Code</summary>
```python
import numpy as np
from flojoy import flojoy, Vector, Scalar


@flojoy
def VECTOR_MAX(default: Vector) -> Scalar:
    """The VECTOR_MAX node returns the maximum value from the Vector

    Inputs
    ------
    v : Vector
        The input vector to use max peration

    Returns
    -------
    Scalar
        The maximum value found from the input vector
    """

    return Scalar(c=np.max(default.v))

```
[Find this Flojoy Block on GitHub](https://github.com/flojoy-ai/blocks/blob/main/blocks/TRANSFORMERS/VECTOR_MANIPULATION/VECTOR_MAX/VECTOR_MAX.py)
</details>

## Example

<GetHelpWidget />

<AppDisplay app={app} blockName="VECTOR_MAX" client:visible />

In this example, we generate a vector starting from 8 and ending at 50 by using a `LINSPACE` node. Then a `VECTOR_MAX` node is connected to the example vector where it will find the maximum value and the one below will find the minimum value. The value is visualized with `BIG_NUMBER` node.
